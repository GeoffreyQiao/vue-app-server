const p_detail = require('./productDetail')
const p_public = require('./productPublic')
const unit = require('./unit')
const spec = require('./spec')
const user = require('./user')

module.exports = {
    p_detail,
    p_public,
    unit,
    spec,
    user
}
